extension DurationExtension on Duration {
  static Duration normalDuration = const Duration(seconds: 1);
  static Duration lowDuration = const Duration(milliseconds: 800);
  static Duration extraLowDuration = const Duration(milliseconds: 600);
  static Duration mediumDuration = const Duration(milliseconds: 1500);
}
